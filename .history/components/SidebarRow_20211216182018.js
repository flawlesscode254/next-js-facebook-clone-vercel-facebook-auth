function SidebarRow({ Icon, title, src }) {
    return (
        <div>
            {src ? (
                <Image />
            ) : (

            )}
        </div>
    )
}

export default SidebarRow
